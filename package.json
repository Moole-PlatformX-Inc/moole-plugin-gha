{
  "name": "moole-security",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action plugin to scan repo for vulnerabilties",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx @vercel/ncc build src/index.js -o dist --license licenses.txt",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}